ol
  - review.each do |i|
    li 
      a href="review/#{i['reviewId']}" #{i["title"]}
      a href="review/#{i['reviewId']}/edit" Uppdatera
      form method="POST" action="review/#{i['reviewId']}/delete"
        input type="submit" value="Delete"
      
            